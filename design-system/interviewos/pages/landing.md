# Landing override

Hero + Features + CTA. Navy hero band, gold primary button. No setup form on this page. Social proof is a single static quote (no auto-rotating carousel). Disable any parallax under reduced motion.
