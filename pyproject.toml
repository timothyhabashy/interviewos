[project]
name = "interviewos"
version = "0.2.0"
description = "InterviewOS — practice environment for high-stakes interviews."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
    { name = "Timothy Habashy", email = "thabashy@mit.edu" },
]

[tool.pytest.ini_options]
testpaths = ["api/tests"]
pythonpath = ["api/src"]
